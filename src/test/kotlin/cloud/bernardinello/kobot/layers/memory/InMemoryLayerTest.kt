package cloud.bernardinello.kobot.layers.memory

import io.kotlintest.specs.StringSpec

class InMemoryLayerTest : StringSpec() {
    init {
//        val config = BotConfig(
//            listOf(StartState("start"), EndState("end")),
//            listOf(BotStateRelationship(from = "start", to = "end"))
//        )
    }
}